# Runtime dependencies for the Lambda bundle.
# boto3 is pre-installed in the AWS Lambda Python runtime but is pinned here so
# local development matches. `anthropic` provides the preferred Bedrock
# transport; the code falls back to boto3's bedrock-runtime if it is absent.
boto3>=1.34.0
anthropic>=0.40.0

# Development only
pytest>=8.0.0
moto[dynamodb]>=5.0.0
PyYAML>=6.0
